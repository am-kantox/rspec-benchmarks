require 'spec_helper'

describe Rspec::Benchmarks do
  it 'has a version number' do
    expect(Rspec::Benchmarks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
